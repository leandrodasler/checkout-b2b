#import "./fragments/SavedCartFields.graphql"

mutation saveCart(
  $title: String
  $additionalData: String
  $parentCartId: String
) {
  saveCart(
    title: $title
    additionalData: $additionalData
    parentCartId: $parentCartId
  ) @context(provider: "ssesandbox04.checkout-b2b") {
    ...SavedCartFields
  }
}
