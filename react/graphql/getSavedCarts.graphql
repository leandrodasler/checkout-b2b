#import "./fragments/SavedCartFields.graphql"

query getSavedCarts($parentCartId: ID) {
  getSavedCarts(parentCartId: $parentCartId)
    @context(provider: "ssesandbox04.checkout-b2b") {
    ...SavedCartFields
  }
}
